[package]
name = "file-type"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.9.2"
strum = { version = "0.27.2", features = ["derive", "strum_macros"] }
users = "0.11.0"

[lints.clippy]
new_without_default = "allow"
